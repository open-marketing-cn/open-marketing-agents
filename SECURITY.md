# 安全策略

## 支持范围

私有审计阶段只修复当前默认分支。公开发布后，最近一个稳定版本和默认分支接受安全修复。

## 报告安全问题

不要在公开 Issue 中提交密钥、客户数据或可利用细节。请通过 GitHub Security Advisory 私下报告；公开仓库启用前，可直接联系仓库维护者并只提供最小复现。

请写明：受影响版本、平台、复现步骤、可能影响和建议缓解方式。不要附带真实客户数据。

## 安装边界

- 只允许固定格式的安装包 ID。
- v0.1 只写入 `~/.codex/skills/open-marketing-*`。
- 安装状态由桌面后端重新校验，不能只信前端按钮。
- 没有真实验证的包不能安装。
- 新增权限必须重新确认。
- 更新或卸载发现用户修改时先备份。
- 远程目录更新默认关闭；只有用户主动检查才联网。

## 数据与凭证

- 桌面应用不收集模型 Key。
- 外部 API Token 保存在用户本机的 Codex/MCP 环境，不写入浏览器包、运行记录或日志。
- Agent 原始数据保存在当前项目 `.open-marketing/`，默认不进入 Git。
- 禁止抓取私信、封闭群、登录后非公开内容或绕过访问控制的数据。

## 贡献者检查

提交前运行：

```bash
npm run safety
npm run verify
cargo test --manifest-path src-tauri/Cargo.toml
```

发布审计还会对完整 Git 历史运行秘密扫描和客户关键词拒绝清单。
