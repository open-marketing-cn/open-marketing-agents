# v0.1 公开仓库审计记录

首次审计：2026-08-13

公开复核：2026-08-14

候选版本：0.1.0-alpha.1

目标：Mac Apple Silicon + Codex

## 已通过

- 仓库为独立 Git 历史，没有继承旧项目记录；
- 工作树与完整 Git 历史拒绝词扫描通过；
- Gitleaks 工作树与完整 Git 历史扫描通过；
- Svelte 类型检查 0 错误、0 警告；
- 前端目录规则测试 5/5 通过；
- Rust 安装器测试 4/4 通过；
- Vite 生产构建通过；
- 仓库不包含历史运行数据、客户数据、外部 Token 或原始媒体；
- 候选 Agent 在真实验证完成前由界面和安装器双重禁止安装。

## 当前公开边界

- GitHub 源代码仓库可以公开；
- 当前版本是 alpha，不代表已有 Agent 通过真实业务验证；
- 不发布未签名、未公证的 `.app` 或 `.dmg`；
- 不把候选数量、Star 或社区关注描述为产品效果；
- 不提交客户资料、账号凭证、群聊截图、原始媒体或可识别的商业数据。

## 正式版本发布前仍需完成

1. 至少一个 Agent 完成“内容版本 × Codex × 实际品类”的真实从业者脱敏任务验证；
2. 将对应包状态更新为 `installable`，并重新运行安装、调用、更新和卸载测试；
3. 完成 Apple Developer ID 签名与公证；
4. 再次检查文件树、Actions、Issue 模板、Release 附件和安装说明；
5. 对远端完整历史重新运行 Gitleaks 和拒绝词扫描。

## 构建产物

构建产物位于被 Git 忽略的 `src-tauri/target/release/bundle/`，不得直接提交进 Git 历史。完成签名和公证后，才可将 DMG 作为 GitHub Release 附件发布。
