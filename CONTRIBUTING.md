# 参与 Open Marketing

Open Marketing 收的是可复用、可验证的营销能力，不是一个只有长提示词的收藏夹。

## 提交路径

1. 先开对应 Issue：Agent 提案、数据连接器、验证报告或 Bug。
2. 写清真实使用者、营销任务、所需数据、系统动作、人工决定、交付物和验收条件。
3. 只提交脱敏样例；不要把客户数据放进 Issue、PR、测试或截图。
4. 完成许可证和来源登记。
5. 通过安全检查、自动测试和 Codex 技术测试。
6. 至少一位真实中国营销从业者完成脱敏任务验证。
7. 由 Open Marketing 维护者审核状态与合并。

## Agent 提案最低要求

- 具体营销任务，而不是宽泛角色名；
- 必需输入、可选输入和可用来源；
- 交付物文件和字段；
- 明确不能推断的内容；
- 缺资料时的停止行为；
- 人工确认点；
- 权限与外部服务；
- 适用行业、平台和工作空间；
- 来源、许可证和本土化改动；
- 验证任务与通过标准。

## 内容来源

- 兼容许可证的上游内容可以在保留归属和修改说明后本土化。
- 许可证不兼容或没有许可证的内容只允许登记链接和分析，不得复制进安装包。
- 多个来源解决同一任务时，合并成一个 Open Marketing 版本并列出所有来源。
- 上游更新只生成“有更新可审阅”，不会自动覆盖本土化版本。

## 客户数据禁止提交

禁止提交真实姓名、头像、昵称、群聊截图、账号 ID、平台 Token、客户路径、私信、内部报告、合同、原始媒体、销售/效果数据或可识别商业关系。测试使用人工编造的数据，并明确标记 `DEMO / 虚构案例`。

## DCO

所有提交必须使用 Developer Certificate of Origin：

```bash
git commit -s -m "feat: add ..."
```

签署代表你有权按仓库许可证提交该内容。完整文本见 [`DCO.md`](DCO.md)。

## 本地检查

```bash
npm run verify
cargo test --manifest-path src-tauri/Cargo.toml
```

PR 应说明实际运行结果，不能只写“应该可用”。
